## Summary

<!-- 1-2 sentences: what changed and why -->

## Testing

- [ ] `bash -n wezterm/repo-launcher.sh` — no syntax errors
- [ ] Tested in a fresh WezTerm window (not the one running the old version)
- [ ] Preview mode still works: `bash wezterm/repo-launcher.sh --preview <REPO>`
- [ ] If statusline changed: tested with a real Claude Code session

## Breaking changes

<!-- Does this change key bindings, config shape, install behavior, or state file format? -->

## Screenshots (if UI)

<!-- Drop terminal screenshots here -->
